const initData = {
    data: []
}

export function sampleReducer(state = initData, action){
    switch (action.type) {
        case '':
            return state;
        default:
            return state;
    }
}
