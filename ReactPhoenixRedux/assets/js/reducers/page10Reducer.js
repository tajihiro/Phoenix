const initData = {
    member_id: 0,
    members: 0
}

export function page10Reducer(state = initData, action){
    switch (action.type) {
        case '':
            return state;
        default:
            return state;
    }
}
