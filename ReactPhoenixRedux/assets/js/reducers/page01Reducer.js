const initData = {
    comments: [],
    isLoading: false,
    hasError: false
}

export function page01Reducer(state = initData, action){
    switch (action.type) {
        case '':
            return state;
        default:
            return state;
    }
}
